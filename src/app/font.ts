import { Poppins, Lato } from "next/font/google";
export const openSans = Poppins({
  weight: ["500", "600"],
  subsets: ["latin"],
});
export const lato = Lato({
  weight: ["300", "400"],
  subsets: ["latin"],
  display: "swap",
});
